version=5.31

source=(http://yaolinux.techozor.fr/sources-base/file-$version.tar.gz)

build() {
cd file-$version
./configure \
    --prefix=/tools \
    --libdir=/tools/lib64 \
    --build=${CLFS_HOST} \
    --host=${CLFS_TARGET}
make
make install
}
