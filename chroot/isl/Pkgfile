version=0.17.1

source=(http://yaolinux.techozor.fr/sources-base/isl-$version.tar.xz)

build() {
cd isl-$version
./configure \
    --prefix=/tools \
    --build=${CLFS_HOST} \
    --host=${CLFS_TARGET} \
    --libdir=/tools/lib64
make
make install
}
