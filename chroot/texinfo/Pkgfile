version=6.3

source=(http://yaolinux.techozor.fr/sources-base/texinfo-$version.tar.xz)

build() {
cd texinfo-$version
PERL=/usr/bin/perl \
./configure \
    --prefix=/tools \
    --build=${CLFS_HOST} \
    --host=${CLFS_TARGET}
make
make install
}
