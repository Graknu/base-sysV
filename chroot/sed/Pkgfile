version=4.4

source=(http://yaolinux.techozor.fr/sources-base/sed-$version.tar.xz)

build() {
cd sed-$version
./configure \
    --prefix=/tools \
    --build=${CLFS_HOST} \
    --host=${CLFS_TARGET}
make
make install
}
