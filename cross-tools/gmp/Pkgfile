version=6.1.2

source=(http://yaolinux.techozor.fr/sources-base/gmp-$version.tar.xz)

build() {
cd gmp-$version
./configure \
    --prefix=/cross-tools \
    --enable-cxx \
    --disable-static
make
make install
}
